defaults:
  - envs: cuda
  - _self_
  - inference: 7b_tp2

experiment:
  exp_name: robobrain2_flaggems
  exp_dir: tests/functional_tests/inference/robobrain2_flaggems/test_results/7b_tp2
  task:
    type: inference
    backend: vllm
    entrypoint: flagscale/inference/inference_robobrain2.py
  runner:
    hostfile: null

action: run

hydra:
  run:
    dir: ${experiment.exp_dir}/hydra
