defaults:
- _self_
- serve: gr00t_n1_5

experiment:
  exp_name: gr00t_n1_5_serve
  exp_dir: outputs/${experiment.exp_name}
  task:
    type: serve
    entrypoint: flagscale/serve/run_serve_gr00t_n1_5.py
  runner:
    hostfile: null
    deploy:
      use_fs_serve: true
  envs:
    CUDA_VISIBLE_DEVICES: 0
    CUDA_DEVICE_MAX_CONNECTIONS: 1

action: run

hydra:
  run:
    dir: ${experiment.exp_dir}/hydra
